---
description: Commit all changes with a descriptive message
---
// turbo-all

1. Stage all changes:
```bash
git add -A
```

2. Commit with an auto-generated descriptive message based on the changes made:
```bash
git commit -m "<descriptive commit message>"
```

3. Confirm the commit was successful by showing the last commit.
