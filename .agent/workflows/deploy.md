---
description: Commit and push all changes to deploy
---
// turbo-all

1. Stage all changes:
```bash
git add -A
```

2. Commit with an auto-generated descriptive message:
```bash
git commit -m "<descriptive commit message>"
```

3. Push to remote:
```bash
git push
```

4. Confirm deployment was successful.
