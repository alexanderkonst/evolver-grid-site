# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (use .env.example as template)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code auto-created session worktrees. Each new "Local Code
# Session" in the Claude Mac app spawns a worktree under
# .claude/worktrees/<random-name>/ — these are personal, ephemeral
# checkouts that should never enter git's index. Without this rule
# the worktree dirs register as gitlinks (mode 160000) and the
# auto-deploy script keeps churning "deploy" commits as they change.
.claude/worktrees/
.gstack/

# Raw LinkedIn exports contain private messages and account data. Keep only
# intentionally derived outreach artifacts under docs/02-strategy/warm-base/.
docs/02-strategy/warm-base/Complete_LinkedInDataExport_*/

# Private data (lives in evolver-private-ledger)
src/generated/crm-snapshot.json
public/generated/crm-snapshot.json
docs/02-strategy/warm-base/
docs/warm_base_platform_users.csv
docs/02-strategy/strategic_crm_outreach_tracker.md
docs/09-logs/project_pulse_log.md
